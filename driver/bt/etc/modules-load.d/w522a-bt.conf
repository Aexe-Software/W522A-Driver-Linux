# Bring up Amlogic W155S1 Bluetooth on boot. Order matters:
# 1) aml_sdio must already be up (it is module-aliased by SDIO probe)
# 2) w522a_bt_reset does the vendor PMU init sequence
# 3) hci_uart binds via serdev (compatible amlogic,w155s2-bt)
aml_sdio
w522a_bt_reset
btintel
btrtl
btbcm
hci_uart
